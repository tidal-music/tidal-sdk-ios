//
// SubscriptionPriceChangeDecisionsAttributes.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SubscriptionPriceChangeDecisionsAttributes: Codable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case accepted = "ACCEPTED"
        case rejected = "REJECTED"
    }
    /** When the user decided. Absent when the consent was implicit (derived from the subscription start). */
    public var decidedAt: Date?
    /** The user's decision. ACCEPTED is permanent; REJECTED can still be flipped to ACCEPTED before the renewal. */
    public var status: Status

    public init(
        decidedAt: Date? = nil,
        status: Status
    ) {
        self.decidedAt = decidedAt
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case decidedAt
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(decidedAt, forKey: .decidedAt)
        try container.encode(status, forKey: .status)
    }
}
