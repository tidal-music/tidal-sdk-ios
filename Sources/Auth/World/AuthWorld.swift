/// - Attention: If logging is not desired, use `.noop` instead of `.live`.
/// ```
/// var AuthWorld = AuthWorldClient.noop
/// ```
var AuthWorld = AuthWorldClient.live // swiftlint:disable:this identifier_name
